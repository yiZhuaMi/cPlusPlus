#include <iostream>
#include <stack>
#include <string>
#include <vector>
using namespace std;

int main()
{
    int q, node, depth;
    cin>>q>>node>>depth;
    vector<vector<int>> v(q,vector<int>(2,-1));
    for (size_t i = 0; i < q; i++)
    {
        cin>>v[i][0]>>v[i][1];
    }
    for (size_t i = 0; i < q; i++)
    {
        int root = 1;
        
    }
    
}